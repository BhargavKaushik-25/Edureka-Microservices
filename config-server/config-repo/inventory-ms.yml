spring:
  application:
    name: inventory-ms
  datasource:
    url: jdbc:mysql://localhost:3306/busdb
    username: bususer
    password: buspass

server:
  port: 8085

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
